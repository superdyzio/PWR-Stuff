#include <stdio.h>
#include <math.h>

int main() 
{
  int bardzo_dluga_zmienna1, bardzo_dluga_zmienna2;
  int a,b;
  float tez_cos_dlugiego;

  while(1)
  {
  //teraz bêdzie du¿o nawiasów
  if ( (a - b) || (a==b) (a) && ((a-b/(s+(a-b)))>0) )
    ;
  //to samo z bledem
  // if ( (a - b) || (a==b) (a) && ((a-b/(s+(a-b)))>0 )
  //  ;

  }

}
